export const theme = {
  primary: {
    color: "black",
    backgroundColor: "white",
  },
  secondary: {
    color: "white",
    backgroundColor: "#121212",
  },
};

export type themetype = {
  primary: {
    color: string;
    backgroundColor: string;
  };
  secondary: {
    color: string;
    backgroundColor: string;
  };
};
